// Fast lane — deterministic phrase table; see docs/booth/CONTRACTS.md §5.
// STUB: real implementation lands in this file (owner: macro/fast-lane task).
export function resolveFast(text, ctx = {}) { return null; }
export async function tryFastLane(text, ctx = {}) { return false; }
